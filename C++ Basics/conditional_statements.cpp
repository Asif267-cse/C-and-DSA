#include <iostream>

using namespace std;

int main() {
    int healthPoints = 50;

    if (healthPoints > 0) {
        cout << "Keep fighting!" << endl;
    } else {
        cout << "Game Over!" << endl;
    }

    char weapon = 's'; // s = sword, b = bow, m = magic

    switch (weapon) {
        case
